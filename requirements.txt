# Core
python-dotenv>=1.0,<2.0
opencv-python>=4.9,<5.0
numpy>=2.0,<3.0

# LangChain (Agent + Tools)
langchain>=0.2,<0.4
langchain-openai>=0.2,<0.3
langchain-community>=0.2,<0.3

# Audio I/O
pyaudio>=0.2.14,<0.3.0
soundfile>=0.12,<1.0

# Speech-to-Text (lokales Whisper über SpeechRecognition)
SpeechRecognition[whisper-local]>=3.10,<4.0
openai-whisper>=20231117

# Piper TTS (lokal, kostenlos)
piper-tts>=1.3,<2.0
huggingface_hub>=0.20,<1.0

# ONNX Runtime (CPU – für Piper, wähle GPU-Variante falls CUDA vorhanden ...oder TensorRT)
# onnxruntime>=1.20.1,<2.0; platform_system=="Windows"
onnxruntime-gpu>=1.20.1,<2.0; platform_system=="Windows"  # optional: falls NVIDIA GPU

# Web-Suche (DuckDuckGo - neues Package 'ddgs')
ddgs>=0.1.0,<1.0

# HTTP-Requests (für eventuelle API-Calls)
requests>=2.31,<3.0

# Testing (optional, for development)
pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<6.0.0